# Creación del repositorio
npx create-next-app@latest roblartech-front --ts --tailwind --eslint --app --src-dir --import-alias "@/*"

create-next-app@16.0.1
Ok to proceed? (y) y 
√ Would you like to use React Compiler? ... No / Yes (Yes)
√ Would you like to use Turbopack? (recommended) ... No / Yes (Yes)

5 minutos

# Subir repo a git

1. Crear repositorio en git, solo poner nombre y crear
2. Seguir las instrucciones para "push an existing repository from the command line"

# Shadcn
npx shadcn@latest init
npx shadcn@latest add --all

shadcn@3.5.0
Ok to proceed? (y) y

Color base
Slate

# Framer motion
npm install framer-motion

# webpack
npm i -D webpack

# Iconify
npm i @iconify/react @iconify-icons/material-symbols

# opcional (tipado estricto recomendado):
npm i -D @iconify/types

# Toaster
npm install @radix-ui/react-toast

# Envio de correos
npm i @getbrevo/brevo


# Despliegue en cloudflare
npm i -D @opennextjs/cloudflare wrangler

(wrangler es opcional)
